for (let i = 0; i < 5;) {
  console.log(i += 1);
}

// logs 1 to 5 in 5 lines. 
// even though the increment is not specified the i counter 
// is incremented in the body of the loop so it workss anyway.